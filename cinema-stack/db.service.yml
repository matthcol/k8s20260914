apiVersion: v1
kind: Service
metadata:
  name: dbmovie
  labels:
    app: dbmovie
spec:
  # type: ClusterIP  # default
  clusterIP: None    # service HEADLESS
  selector:
    app: dbmovie
  ports:
  - port: 5432
    targetPort: 5432
